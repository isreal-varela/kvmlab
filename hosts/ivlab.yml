# file: hosts/ivlab.yml

tang_host:

ipa_host:
  hosts:
    ipa1

ipa_client:

logger_client:

nbde_servers:

nbde_clients:

kvms:
  vars:
    ansible_connection: ssh

containers:
  vars:
    ansible_connection=podman
    ansible_python_interpreter=auto_silent
  children:

all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    dns_name: 'ape.test'
    env_label: 'Unclassified'
    env_colorbar: '#067040'
    # foreman_host: 'foreman.ape.test'
    frmn_host: "foreman.{{ dns_name }}"
    frmn_url: "https://{{ frmn_host }}"
    inet_access: true
    kvm_list:
      - {name: "ipa1",       ip_addr: "192.168.100.40", enabled: true,  roles: ["ipa_host",      "logger_client"], cpus: 2, ram_mb: 4096}
      - {name: "tang",       ip_addr: "192.168.100.41", enabled: false, roles: ["tang_host",     "ipa_client", "logger_client"], ram_mb: 3072}
      - {name: "vault",      ip_addr: "192.168.100.42", enabled: true,  roles: ["vault_host",    "ipa_client", "logger_client"]}
      - {name: "logger",     ip_addr: "192.168.100.43", enabled: true,  roles: ["logger_host",   "ipa_client"]}
      - {name: "foreman",    ip_addr: "192.168.100.44", enabled: true,  roles: ["foreman_host",  "ipa_client", "logger_client"], cpus: 4, ram_mb: 20480, rt_disk: '120G'}
      - {name: "mirror-reg", ip_addr: "192.168.100.45", enabled: true,  roles: ["registry_host", "ipa_client", "logger_client"], rt_disk: '120G'}

