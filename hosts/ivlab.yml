# file: inventories/ivlab.yml

tang_hosts:

ipa_hosts:

nbde_servers:

nbde_clients:

kvms:
  vars:
    ansible_connection: ssh
  children:
    tang_hosts
    ipa_hosts

containers:
  vars:
    ansible_connection=podman
    ansible_python_interpreter=auto_silent
  children:

all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    kvm_list:
      # - {name: 'tang',       ip_addr: '192.168.100.41', enabled: true,  roles: ['tang_host',     'nbde_servers'], ram_mb: 3072}
      # - {name: 'ipa1',       ip_addr: '192.168.100.40', enabled: true,  roles: ['ipa_host',      'nbde_clients'], cpus: 2, ram_mb: 4096}
      # - {name: 'vault',      ip_addr: '192.168.100.42', enabled: true,  roles: ['vault_host',    'nbde_clients']}
      - {name: 'logger',     ip_addr: '192.168.100.43', enabled: true,  roles: ['logger_host',   'nbde_clients']}
      - {name: 'foreman',    ip_addr: '192.168.100.44', enabled: true,  roles: ['foreman_host',  'nbde_clients'], cpus: 1}
      # - {name: 'mirror-reg', ip_addr: '192.168.100.45', enabled: true,  roles: ['registry_host', 'nbde_clients']}

