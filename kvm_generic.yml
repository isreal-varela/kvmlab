---
- hosts: kvms
  
  tasks:
    - name: "Gather facts"
      setup:
        gather_subset: all

    - name: "Upload master hosts file to each KVM"
      become: true
      copy:
        src: /etc/hosts
        dest: /etc/hosts
        owner: root
        group: root
        mode: '0644'
        backup: yes

